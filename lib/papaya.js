// var $ = require('jquery');
// do we need to require jquery here?

function Papaya (x, y, width, height) {
  this.x = x;
  this.y = y;
  this.width = width;
  this.height = height;
}
// is it appropriate to have a constructor function for one item like a ball/papaya-whip?
// for example, should we use the above or below?

// var papaya = {
//   x: x,
//   y: y,
//   width: width,
//   height: height
// };
